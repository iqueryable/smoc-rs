pub fn setup(repository: String, template: String) {
    // ensure config does not exist
    // install templates from repository
    // create config file

    println!(
        "Init project from {} with template {}",
        repository, template
    )
}

pub fn update() {
    // ensure config exists
    // ensure no changes in the working directory
    // map templates variables
    // run templates in temporary repository
    // update config file
    // merge change revision into working directory

    println!("Update project")
}
